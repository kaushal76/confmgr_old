{% extends 'base.html.twig' %}

{% block body %}
    <h1>ConfmgrAuthorPaper</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ confmgrAuthorPaper.id }}</td>
            </tr>
            <tr>
                <th>Apposition</th>
                <td>{{ confmgrAuthorPaper.apPosition }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('authorpaper_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('authorpaper_edit', { 'id': confmgrAuthorPaper.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
